export default function LandingModelWrapper() {
    return (
        <div>
            {/*
             * TODO: Cargar modelo 3D aquí con React Three Fiber.
             *
             * Pasos sugeridos cuando el modelo esté listo:
             *   1. Instalar dependencias:
             *        pnpm add three @react-three/fiber @react-three/drei
             *   2. Agregar "use client" al inicio de este archivo (R3F requiere cliente).
             *   3. Reemplazar este div con un <Canvas> de @react-three/fiber.
             *   4. Cargar el modelo con useGLTF('/ruta/del/modelo.glb') de @react-three/drei.
             *
             * Ejemplo mínimo:
             *   import { Canvas } from '@react-three/fiber'
             *   import { useGLTF, OrbitControls } from '@react-three/drei'
             *
             *   function Model() {
             *     const { scene } = useGLTF('/models/tilekick.glb')
             *     return <primitive object={scene} />
             *   }
             *
             *   <Canvas camera={{ position: [0, 2, 5] }}>
             *     <ambientLight intensity={0.5} />
             *     <directionalLight position={[10, 10, 5]} />
             *     <Model />
             *     <OrbitControls enableZoom={false} />
             *   </Canvas>
             */}
        </div>
    );
}